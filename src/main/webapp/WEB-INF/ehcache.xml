<?xml version="1.0" encoding="UTF-8"?>
<ehcache updateCheck="false" monitoring="autodetect" dynamicConfig="false">

    <diskStore path="java.io.tmpdir"/>

    <defaultCache
           maxElementsInMemory="1000"
           eternal="true"
           timeToIdleSeconds="3600"
           timeToLiveSeconds="3600"
           overflowToDisk="true"
           diskSpoolBufferSizeMB="1" />


    <!-- newstype -->
    <cache name="newstype" maxElementsInMemory="1000"/>

    <!-- news -->
    <cache name="news" maxElementsInMemory="1000"/>

    <!-- product -->
    <cache name="product" maxElementsInMemory="1000"/>

    <!-- ad -->
    <cache name="ad" maxElementsInMemory="1000"/>

    <!-- link -->
    <cache name="link" maxElementsInMemory="1000"/>

    <!-- comment -->
    <cache name="comment" maxElementsInMemory="1000"/>

    <!-- job -->
    <cache name="job" maxElementsInMemory="1000"/>

    <!-- webmenu -->
    <cache name="webmenu" maxElementsInMemory="1000"/>

    <!-- webinfo -->
    <cache name="webinfo" maxElementsInMemory="1000"/>

    <!-- user -->
    <cache name="user" maxElementsInMemory="1000"/>

    <!-- menu -->
    <cache name="menu" maxElementsInMemory="1000"/>

    <!-- info -->
    <cache name="info" maxElementsInMemory="1000"/>

</ehcache>